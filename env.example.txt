# Hackathon Chatbot Environment Variables
# Copy this file to .env.local and configure your preferred LLM provider(s)

# =============================================================================
# LLM PROVIDER CONFIGURATION
# =============================================================================
# The system will automatically detect and use the first available provider
# in this priority order: OpenAI -> Anthropic -> Gemini -> Apigee -> Enterprise -> Local -> Ollama

# -----------------------------------------------------------------------------
# OpenAI Configuration
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_TEMPERATURE=0.1
OPENAI_MAX_TOKENS=4000
OPENAI_TIMEOUT=600000

# -----------------------------------------------------------------------------
# Anthropic Configuration
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-3-sonnet-20240229
ANTHROPIC_BASE_URL=https://api.anthropic.com/v1
ANTHROPIC_TEMPERATURE=0.1
ANTHROPIC_MAX_TOKENS=4000
ANTHROPIC_TIMEOUT=600000

# -----------------------------------------------------------------------------
# Google Gemini Configuration
# -----------------------------------------------------------------------------
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-pro
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_TEMPERATURE=0.1
GEMINI_MAX_TOKENS=4000
GEMINI_TIMEOUT=600000

# -----------------------------------------------------------------------------
# Local LLM Configuration (e.g., LM Studio, Ollama, etc.)
# -----------------------------------------------------------------------------
LOCAL_LLM_URL=http://localhost:11434/v1
LOCAL_LLM_MODEL=llama-3.2-3b-instruct
LOCAL_LLM_API_KEY=not-needed
LOCAL_LLM_TEMPERATURE=0.1
LOCAL_LLM_MAX_TOKENS=4000
LOCAL_LLM_TIMEOUT=600000

# -----------------------------------------------------------------------------
# Ollama Configuration
# -----------------------------------------------------------------------------
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama-3.2-3b-instruct
OLLAMA_TEMPERATURE=0.1
OLLAMA_NUM_PREDICT=4000
OLLAMA_TIMEOUT=600000

# -----------------------------------------------------------------------------
# Enterprise LLM Configuration
# -----------------------------------------------------------------------------
ENTERPRISE_LLM_URL=https://your-enterprise-llm-endpoint.com
ENTERPRISE_LLM_MODEL=llama-3.2-3b-instruct
ENTERPRISE_LLM_API_KEY=your_enterprise_api_key
ENTERPRISE_LLM_TOKEN=your_initial_bearer_token
ENTERPRISE_LLM_TOKEN_EXPIRY_HOURS=24
ENTERPRISE_LLM_REFRESH_URL=https://your-token-refresh-endpoint.com
ENTERPRISE_LLM_CLIENT_ID=your_client_id
ENTERPRISE_LLM_CLIENT_SECRET=your_client_secret
ENTERPRISE_LLM_REFRESH_TOKEN=your_refresh_token
ENTERPRISE_LLM_HEADERS={"X-Custom-Header": "value"}
ENTERPRISE_LLM_TEMPERATURE=0.1
ENTERPRISE_LLM_MAX_TOKENS=4000
ENTERPRISE_LLM_TIMEOUT=600000

# -----------------------------------------------------------------------------
# Apigee Enterprise Configuration
# -----------------------------------------------------------------------------
APIGEE_NONPROD_LOGIN_URL=https://your-apigee-login-endpoint.com/oauth/token
APIGEE_CONSUMER_KEY=your_apigee_consumer_key
APIGEE_CONSUMER_SECRET=your_apigee_consumer_secret
APIGEE_MODEL=gpt-4
APIGEE_TEMPERATURE=0.1
APIGEE_MAX_TOKENS=4000
APIGEE_TIMEOUT=600000

# Enterprise base URL for Apigee-authenticated requests
ENTERPRISE_BASE_URL=https://your-enterprise-base-url.com
WF_USE_CASE_ID=your_use_case_id
WF_CLIENT_ID=your_wf_client_id
WF_API_KEY=your_wf_api_key

# =============================================================================
# LEGACY CONFIGURATION (for backward compatibility)
# =============================================================================
# LM Studio URL (fallback when other providers fail)
LM_STUDIO_URL=http://localhost:1234/v1/chat/completions

# =============================================================================
# QUICK START INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local
# 2. Configure at least ONE LLM provider above
# 3. For local testing, you can use:
#    - LM Studio: Set LM_STUDIO_URL and ensure LM Studio is running
#    - Ollama: Set OLLAMA_HOST and ensure Ollama is running
# 4. For cloud providers, get API keys and set the appropriate variables
# 5. Start the application with: npm run dev

# =============================================================================
# PROVIDER PRIORITY
# =============================================================================
# The system automatically detects available providers in this order:
# 1. OpenAI (if OPENAI_API_KEY is set)
# 2. Anthropic (if ANTHROPIC_API_KEY is set)
# 3. Gemini (if GEMINI_API_KEY is set)
# 4. Apigee (if APIGEE_NONPROD_LOGIN_URL is set)
# 5. Enterprise (if ENTERPRISE_LLM_URL is set)
# 6. Local (if LOCAL_LLM_URL is set)
# 7. Ollama (if OLLAMA_HOST is set)
# 8. Fallback to LM Studio (if LM_STUDIO_URL is set)

# =============================================================================
# TESTING
# =============================================================================
# Test available providers: GET /api/llm/providers
# Test specific provider: POST /api/llm/providers with {"provider": "openai"}
